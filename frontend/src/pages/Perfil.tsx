import React, { useState } from 'react';
import PerfilModal from '../components/PerfilModal';

export default function PerfilPage() {
  return (
    <div className="container py-5 text-center perfil-page-container">
      Nenhum usuário simulado. Implemente a lógica de autenticação para exibir o perfil real.
    </div>
  );
}
