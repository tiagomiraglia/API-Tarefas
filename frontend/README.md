# Frontend (Landing Login)

Passos rápidos:

- Instalar dependências:

```powershell
cd frontend; npm install
```

- Rodar em modo desenvolvimento (porta 3000):

```powershell
npm run dev
```

Observações: o projeto usa Vite + React + TypeScript. A rota raiz mostra a landing page de login.
